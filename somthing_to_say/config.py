"""集中配置：所有环境适配相关的常量都放这里。

设计原则：可能随部署环境变化的量（路径、API 配置、业务常量）统一在此定义，
其他模块从这里 import，避免散落硬编码。换环境只需改这一个文件。
"""

from pathlib import Path

from dotenv import load_dotenv

# ── 环境变量加载 ──
# find_dotenv() 从当前目录向上查找 .env，保证从任意子目录启动都能加载
load_dotenv()

# ── 路径 ──
APP_DIR = Path(__file__).resolve().parent

CHROMA_PERSIST_DIR = str(APP_DIR / "chroma_store")     # 向量库持久化目录
FAQ_DOCS_DIR = str(APP_DIR / "faq_docs")               # FAQ 知识库文档目录
DB_PATH = str(APP_DIR / "customer_service.db")          # SQLite 数据库文件
STATIC_DIR = str(APP_DIR / "static")                   # 前端静态文件目录

# ── 郑州大学只读 MCP ──
ZZU_OFFICIAL_DOMAIN = "zzu.edu.cn"
ZZU_SEARCH_URL = "https://www.zzu.edu.cn/ssjgy.jsp?wbtreeid=1001"
ZZU_HTTP_TIMEOUT_SECONDS = 10.0
ZZU_MAX_RESPONSE_BYTES = 1_000_000
ZZU_MAX_REDIRECTS = 3
ZZU_OFFICIAL_SOURCES = (
    ("郑州大学首页", "https://www.zzu.edu.cn/", "学校新闻与公开信息"),
    ("本科生院", "http://www5.zzu.edu.cn/jwc/", "选课、教务与教学通知"),
    ("研究生院", "http://gs.zzu.edu.cn/", "研究生培养与管理通知"),
    ("学生工作", "https://www.zzu.edu.cn/rcpy/xsgzwz.htm", "学生事务入口"),
)

# ── 模型 ──
PRIMARY_MODEL = "deepseek:deepseek-v4-flash"  # DeepSeek 主模型
FALLBACK_MODEL = "deepseek:deepseek-v4-pro"   # 降级/摘要模型

# ── 中间件参数（业务决策，用户自定义）──
BANNED_WORDS = ["炸药", "诈骗", "毒品", "违禁词1", "违禁词2"]  # 输入拦截词表
PII_TYPE = "email"         # PII 脱敏类型
MAX_RETRIES = 3            # 模型重试次数
SUMMARY_TRIGGER_TOKENS = 3000  # 历史超过多少 token 触发摘要

# ── 熔断（Circuit Breaker）──
# 连续失败达到阈值后，一段时间内直接短路，不再调用模型，服务快速失败。
CIRCUIT_FAILURE_THRESHOLD = 3    # 连续失败多少次后熔断打开
CIRCUIT_COOL_DOWN_SECONDS = 30.0 # 熔断打开后多久进入半开试探
CIRCUIT_OPEN_MESSAGE = "我刚才没能接上，你可以再试一次。需要的话，也可以先找真人聊聊。"

# ── RAG ──

# ── RAG ──
RAG_K = 3                          # 检索返回条数
CHUNK_SIZE = 200                   # 文本切分块大小
CHUNK_OVERLAP = 20                 # 切分重叠
COLLECTION_NAME = "customer_faq"   # 向量库集合名

# 检索置信度阈值（方案一：防幻觉）
# 含义：检索分数（Chroma 中为 1 - 欧氏距离）低于该值视为「知识库没有答案」，
#       工具会返回如实的无答案提示，避免 Agent 拿着不相关上下文硬编答案。
# 标定方法：运行 scripts/calibrate_threshold.py 看真实问题/无关问题的分数分布，
#          一般选「能正确回答问题的分数」与「明显无关的分数」之间的值，再留点余量。
RAG_SCORE_THRESHOLD = 0.5

# ── 系统提示词（情绪陪伴人设，业务决策）──
SYSTEM_PROMPT = (
    "你是大学生情绪陪伴助手“有话说”。你的职责是提供温和、诚实、有边界的陪伴。\n"
    "- 面对情绪表达时，先回应用户具体表达出的感受，认真倾听并复述你的理解，再考虑建议。\n"
    "- 询问用户更需要倾听、梳理还是建议，并尊重用户当下的选择。\n"
    "- 不进行心理疾病诊断或治疗，不承诺治疗效果，也不冒充心理咨询师、辅导员或任何真人。\n"
    "- 不要把普通的难过、压力或低落夸大为严重问题。\n"
    "- 如果困扰持续并影响学习、睡眠、饮食或日常功能，可以建议用户自行打开页面上的"
    "“找真人聊聊”，查看经过核实的学校支持或其他官方帮助渠道。\n"
    "- 当用户明确表达自伤、自杀或伤害他人的意图、计划、时间或方法，或正在遭受暴力或"
    "严重失控时，停止普通建议，先确认用户当前是否安全，再建议联系可信任的身边人、"
    "经过核实的学校支持渠道或当地紧急服务。\n"
    "- 你只能提供帮助入口，绝不自动创建工单、发送聊天内容或代替用户联系任何人，"
    "也不能声称已经联系任何人。\n"
    "- 对郑州大学校内流程、通知、联系方式等事实性问题，可以使用“郑州大学官网公开资料”工具；"
    "只能依据工具返回的内容回答，并引用工具返回的官方链接。\n"
    "- 工具未返回可靠资料时，明确说明未检索到官方依据，不要猜测或编造校内规定、日期、"
    "联系方式或办理流程。\n"
    "- 情绪陪伴对话仅在用户明确提出且与相应功能匹配时使用天气、订单、FAQ 或校园资料工具；"
    "FAQ 或校园资料没有可靠答案时如实说明，不要编造。"
)

PROBLEM_SOLVING_PROMPT = (
    "你是大学生问题解决助手“有话说”。用户选择了“帮我想办法”，你的任务是给出及时、具体、"
    "安全且今天能开始执行的建议，而不是只表达安慰。\n"
    "- 直接回应用户当前的问题和目标，不强制固定格式，也不重复套用同一种开头。\n"
    "- 信息不足时只追问最关键的一点；信息足够时给出少量现实、可执行的选择。\n"
    "- 需要比较时说明适用条件或代价；用户需要行动建议时给出容易开始的一步。\n"
    "- 回答保持简洁，避免用长篇情绪安慰代替问题分析。\n"
    "- 不提供医疗诊断或治疗、法律定论、投资建议、违法操作或危险操作指导。\n"
    "- 对郑州大学校内流程、通知、联系方式等事实性问题，可以使用“郑州大学官网公开资料”工具；"
    "只能依据工具返回的内容回答，并引用工具返回的官方链接。\n"
    "- 工具未返回可靠资料时，明确说明未检索到官方依据，不要猜测或编造校内规定、日期、"
    "联系方式或办理流程。\n"
    "- 如果用户表达自伤、伤人、暴力或正在失控，停止普通方案，优先确认安全并建议联系可信任的人、"
    "学校官方支持渠道或当地紧急服务。只提供入口，不代替用户联系任何人。"
)
